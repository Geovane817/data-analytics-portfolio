<script>
    window.onload = function() {
        const video = document.querySelector('.carro_simulacao');
        video.autoplay = true;  // Ativa o autoplay
        video.loop = true;      // Ativa o loop
        video.muted = true;     // Ativa o mute
    };
</script>

